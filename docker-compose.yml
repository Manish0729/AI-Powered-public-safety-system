l host link is not working 
version: "3.9"
services:
  api:
    build: .
    container_name: safety_api
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET=change-me
      - PRIVACY_SALT=please-change-me
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/safety
      - RUN_CROWD_WORKER=false
      - CAMERA_ID=camera-1
      - VIDEO_SOURCE=/app/samples/demo.mp4
      - YOLO_MODEL=yolov8n.pt
      - CROWD_PUBLISH_INTERVAL=1.0
    depends_on:
      - redis
      - db
    volumes:
      - ./backend:/app
  redis:
  db:
    image: postgres:15-alpine
    container_name: safety_db
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=safety
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    image: redis:7-alpine
    container_name: safety_redis
    ports:
      - "6379:6379"
  frontend:
    image: node:20-alpine
    container_name: safety_frontend
    working_dir: /usr/src/app
    volumes:
      - ./frontend:/usr/src/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:8000
      - VITE_WS_URL=ws://localhost:8000/ws/alerts
    depends_on:
      - api
volumes:
  db_data:
